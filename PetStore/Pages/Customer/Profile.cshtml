@page "/Profile/{id}"
@model PetStore.Pages.Customer.ProfileModel
@{

}
<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="utf-8">
    <title>Restoran - Bootstrap Restaurant Template</title>
    <meta content="width=device-width, initial-scale=1.0" name="viewport">
    <meta content="FurFriends" name="keywords">
    <meta content="FurFriends - Vì yêu thương không chỉ nói bằng lời" name="description">

    <!-- Favicon -->
    <link href="~/img/favicon.ico" rel="icon">

    <!-- Google Web Fonts -->
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Heebo:wght@400;500;600&family=Nunito:wght@600;700;800&family=Pacifico&display=swap" rel="stylesheet">

    <!-- Icon Font Stylesheet -->
    <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/5.10.0/css/all.min.css" rel="stylesheet">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.4.1/font/bootstrap-icons.css" rel="stylesheet">

    <!-- Libraries Stylesheet -->
    <link href="~/lib/animate/animate.min.css" rel="stylesheet">
    <link href="~/lib/owlcarousel/assets/owl.carousel.min.css" rel="stylesheet">
    <link href="~/lib/tempusdominus/css/tempusdominus-bootstrap-4.min.css" rel="stylesheet" />

    <!-- Customized Bootstrap Stylesheet -->
    <link href="~/css/bootstrap.min.css" rel="stylesheet">

    <!-- Template Stylesheet -->
    <link href="~/css/style.css" rel="stylesheet">
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
</head>

<body>
    <div class="container-xxl bg-white p-0">


        <!-- Back to Top -->
        <a href="#" class="btn btn-lg btn-primary btn-lg-square back-to-top"><i class="bi bi-arrow-up"></i></a>
    </div>

    <!-- JavaScript Libraries -->
    <script src="https://code.jquery.com/jquery-3.4.1.min.js"></script>
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.0.0/dist/js/bootstrap.bundle.min.js"></script>
    <script src="~/lib/wow/wow.min.js"></script>
    <script src="~/lib/easing/easing.min.js"></script>
    <script src="~/lib/waypoints/waypoints.min.js"></script>
    <script src="~/lib/counterup/counterup.min.js"></script>
    <script src="~/lib/owlcarousel/owl.carousel.min.js"></script>
    <script src="~/lib/tempusdominus/js/moment.min.js"></script>
    <script src="~/lib/tempusdominus/js/moment-timezone.min.js"></script>
    <script src="~/lib/tempusdominus/js/tempusdominus-bootstrap-4.min.js"></script>
    <div class="container-xxl bg-white p-0 profile-page">

        <div class="container profile-container col-lg-7 col-md-7 col-xs-8 col-sm-8">
            @if(Model.AccountId == 0)
            {
                <h1>Hồ sơ người dùng</h1>
            }
            else
            {
                <h1>Trang cá nhân</h1>
            }
            <p class="text-danger" style="text-align:center;">@TempData["error"]</p>
            @if (Model.InformationUser != null)
            {
                <div class="profile-header">
                    <!-- Ảnh đại diện bên phải -->
                    @if (Model.InformationUser.Image != null)
                    {
                        <img src="@Model.InformationUser.Image" alt="Profile Picture" class="col-lg-6 col-md-6 col-xs-12 col-sm-12">
                    }
                    else
                    {
                        @if (Model.InformationUser.Gender == "Male")
                        {
                            <img src="https://vietnampfa.com/wp-content/uploads/2024/10/avatar-mac-dinh-30xJKPDu.jpg" alt="Profile Picture">
                        }
                        else
                        {
                            <img src="https://seoweb.danang.vn/wp-content/uploads/2023/06/avatar-mac-dinh-facebook-nu.jpg" alt="Profile Picture">
                        }
                    }
                    @if (Model.InformationUser != null)
                    {
                        <!-- Thông tin hồ sơ bên trái -->
                        <div class="profile-info col-lg-6 col-md-6 col-xs-12 col-sm-12">
                            <p><span>Họ và tên:</span> @Model.InformationUser.Fullname </p>
                            @if (Model.AccountId == 0)
                            {
                                <p><span>Email:</span> @Model.InformationUser.Account.Email</p>
                                <p><span>Số điện thoại:</span> @Model.InformationUser.Phone</p>
                            }
                            <p><span>Giới tính:</span> @Model.InformationUser.Gender</p>
                            @if (Model.AccountId == 0)
                            {
                                <p><span>Địa chỉ:</span> @Model.InformationUser.Address</p>
                            }
                        </div>
                    }
                    else
                    {
                        <p>Không tìm thấy thông tin tài khoản.</p>
                    }
                    @if (Model.notificationMessage != null)
                    {
                        <div class="col-lg-6 col-md-6 col-xs-12 col-sm-12">
                            <span style="color: forestgreen">
                                @Model.notificationMessage
                            </span>
                        </div>
                    }
                </div>
            }
            else
            {
                <div class="profile-header">
                    <!-- Ảnh đại diện bên phải -->
                    <img src="https://vietnampfa.com/wp-content/uploads/2024/10/avatar-mac-dinh-30xJKPDu.jpg" alt="Profile Picture">
                    @if (Model.InformationUser == null)
                    {
                        <!-- Thông tin hồ sơ bên trái -->
                        <div class="profile-info">
                            <p><span>Họ và tên:</span></p>
                            <p><span>Email:</span> @Model.UserAdditionInfo.Email</p>
                            <p><span>Số điện thoại:</span> </p>
                            <p><span>Giới tính:</span></p>
                        </div>
                    }
                    @if (Model.notificationMessage != null)
                    {
                        <div>
                            <span style="color: forestgreen">
                                @Model.notificationMessage
                            </span>
                        </div>
                    }
                </div>
            }

            @if (Model.AccountId == 0)
            {
                <div style="text-align: right;">
                    <button id="updateProfileCloseBtn" class="btn btn-primary py-2 px-4">Cập nhật</button>
                </div>
            }

            @if (Model.InformationUser != null)
            {
                <!-- Hidden Update Form -->
                <div id="updateProfileForm" style="display:none;">
                    <div class="container border p-4 rounded shadow-lg bg-white" style="max-width: 900px; opacity: 0.9;">
                        <div class="row justify-content-center">
                            <div class="container">
                                <h5 class="section-title ff-secondary text-center text-primary fw-normal mb-4">Cập nhật thông tin của bạn</h5>
                                <form method="post" enctype="multipart/form-data">
                                    <div class="row">
                                        <!-- Cột trái -->
                                        <div class="col-lg-6">
                                            <div class="mb-3">
                                                <label for="FullName" class="form-label fw-bold">Họ và tên <span class="text-danger"> * </span></label>
                                                <input type="text" class="form-control" id="Fullname" name="fullname" value="@(Model.InformationUser.Fullname ?? "")" required>
                                                <small id="fullnameError" style="color: red; display: none;">Tên đầy đủ không được chứa số hoặc ký tự đặc biệt và phải có ít nhất 6 ký tự.</small>
                                            </div>
                                            <div class="mb-3">
                                                <label for="Phone" class="form-label fw-bold">Số điện thoại</label>
                                                <input type="number" class="form-control" id="Phone" name="phone" value="@(Model.InformationUser.Phone ?? "")">
                                                <small id="phoneError" style="color: red; display: none;">Số điện thoại phải có 10 chữ số và bắt đầu bằng 09, 08, 03, 07 hoặc 05.</small>
                                            </div>
                                            <div class="mb-3">
                                                <label class="form-label fw-bold">Giới tính</label>
                                                <div class="d-flex align-items-center">
                                                    <div class="me-3">
                                                        <input type="radio" id="genderMale" name="gender" value="Male"
                                                        @if (Model.InformationUser.Gender == "Male")
                                                        {
                                                            <text>checked</text>
                                                        } required>
                                                        <label for="genderMale">Male</label>
                                                    </div>
                                                    <div>
                                                        <input type="radio" id="genderFemale" name="gender" value="Female"
                                                        @if (Model.InformationUser.Gender == "Female")
                                                        {
                                                            <text>checked</text>
                                                        } required>
                                                        <label for="genderFemale">Female</label>
                                                    </div>
                                                </div>

                                            </div>
                                        </div>
                                        <!-- Cột phải -->
                                        <div class="col-lg-6">
                                            <div class="mb-3">
                                                <label for="Address" class="form-label fw-bold">Địa chỉ</label>
                                                <input type="text" class="form-control" id="Address" name="address" value="@(Model.InformationUser.Address ?? "")">
                                                <small id="addressError" style="color: red; display: none;">Địa chỉ phải dài hơn 10 ký tự và không được chứa ký tự đặc biệt.</small>
                                            </div>
                                            <div class="mb-3">
                                                <label for="Image" class="form-label fw-bold">Ảnh đại diện</label>
                                                <input type="file" class="form-control" id="Image" name="image" value="">
                                            </div>
                                        </div>
                                    </div>
                                    <div class="text-center mt-3">
                                        <button type="submit" class="btn btn-primary py-2 px-4" id="updateProfileBtn">Cập nhật</button>
                                    </div>
                                </form>
                            </div>

                        </div>
                    </div>
                </div>
            }
            else
            {
                <!-- Hidden Update Form -->
                <div id="updateProfileForm" style="display:none;">
                    <div class="container border p-4 rounded shadow-lg bg-white" style="max-width: 900px; opacity: 0.9;">
                        <div class="row justify-content-center">
                            <div class="container">
                                <h5 class="section-title ff-secondary text-center text-primary fw-normal mb-4">Cập nhật thông tin của bạn</h5>
                                <form method="post" enctype="multipart/form-data">
                                    <div class="row">
                                        <!-- Cột trái -->
                                        <div class="col-lg-6">
                                            <div class="mb-3">
                                                <label for="FullName" class="form-label fw-bold">Họ và tên <span class="text-danger"> * </span></label>
                                                <input type="text" class="form-control" id="Fullname" name="fullname" value="" required>
                                                <small id="fullnameError" style="color: red; display: none;">Tên đầy đủ không được chứa số hoặc ký tự đặc biệt và phải có ít nhất 6 ký tự.</small>
                                            </div>
                                            <div class="mb-3">
                                                <label for="Phone" class="form-label fw-bold">Số điện thoại</label>
                                                <input type="number" class="form-control" id="Phone" name="phone" value="">
                                                <small id="phoneError" style="color: red; display: none;">Số điện thoại phải có 10 chữ số và bắt đầu bằng 09, 08, 03, 07 hoặc 05.</small>
                                            </div>
                                            <div class="mb-3">
                                                <label class="form-label fw-bold">Giới tính</label>
                                                <div class="d-flex align-items-center">
                                                    <div class="me-3">
                                                        <input type="radio" id="genderMale" name="gender" value="Male" checked />
                                                        <label for="genderMale">Male</label>
                                                    </div>
                                                    <div>
                                                        <input type="radio" id="genderFemale" name="gender" value="Female" />
                                                        <label for="genderFemale">Female</label>
                                                    </div>
                                                </div>

                                            </div>
                                        </div>
                                        <!-- Cột phải -->
                                        <div class="col-lg-6">
                                            <div class="mb-3">
                                                <label for="Address" class="form-label fw-bold">Địa chỉ</label>
                                                <input type="text" class="form-control" id="Address" name="address" value="">
                                                <small id="addressError" style="color: red; display: none;">Địa chỉ phải dài hơn 10 ký tự và không được chứa ký tự đặc biệt.</small>
                                            </div>
                                            <div class="mb-3">
                                                <label for="Image" class="form-label fw-bold">Ảnh đại diện</label>
                                                <input type="file" class="form-control" id="Image" name="image" value="">
                                            </div>
                                        </div>
                                    </div>
                                    <div class="text-center mt-3">
                                        <button type="submit" class="btn btn-primary py-2 px-4" id="updateProfileBtn">Cập nhật</button>
                                    </div>
                                </form>
                            </div>

                        </div>
                    </div>
                </div>
            }
        </div>
        @if (Model.lsForum != null)
        {
            <div class="container profile-container  col-lg-12 col-md-12 col-xs-12 col-sm-12">
                @if (Model.AccountId == 0)
                {
                    <h3>Bài đăng của bạn</h3>
                }
                else
                {
                    <h3>Bài đăng của @Model.InformationUser.Fullname</h3>
                }
                <div>
                    @foreach (var i in Model.lsForum)
                    {
                        <div class="shadow forum-item" style="width: 100%; border-radius: 12px; margin-bottom: 10px; position: relative;">
                            <a href="/Forum/@i.ForumId" style="color:black">
                                @if (i.Image != null)
                                {
                                    <div style="padding: 22px;">
                                        <img src="@i.Image" style="width: 100%;" />
                                    </div>
                                }
                                <div class="text-start" style="padding:0 22px; padding-top:10px">
                                    <div style="display: flex; justify-content: space-between; align-items: center;">
                                        <h5 style="font-weight: bold; margin-bottom: 20px;">@i.Title</h5>
                                        <div style="display: flex; gap: 8px; align-items: center;">
                                            <!-- Nút Ghim -->
                                            @if (ViewContext.HttpContext.Session.GetInt32("acc") == 1)
                                            {
                                                <form method="post" asp-page-handler="Pin">
                                                    @Html.AntiForgeryToken()
                                                    <input name="id" hidden value="@i.ForumId" />
                                                    <button class="pin-button" type="submit" data-id="@i.ForumId"
                                                            style="border: none; background-color: transparent; cursor: pointer;">
                                                        @if(i.IsPinned == true){
                                                            <svg xmlns="http://www.w3.org/2000/svg" width="25" height="25" fill="red" class="bi bi-pin-angle-fill" viewBox="0 0 16 16">
                                                                <path d="M9.828.722a.5.5 0 0 1 .354.146l4.95 4.95a.5.5 0 0 1 0 .707c-.48.48-1.072.588-1.503.588-.177 0-.335-.018-.46-.039l-3.134 3.134a6 6 0 0 1 .16 1.013c.046.702-.032 1.687-.72 2.375a.5.5 0 0 1-.707 0l-2.829-2.828-3.182 3.182c-.195.195-1.219.902-1.414.707s.512-1.22.707-1.414l3.182-3.182-2.828-2.829a.5.5 0 0 1 0-.707c.688-.688 1.673-.767 2.375-.72a6 6 0 0 1 1.013.16l3.134-3.133a3 3 0 0 1-.04-.461c0-.43.108-1.022.589-1.503a.5.5 0 0 1 .353-.146" />
                                                            </svg>
                                                        }else{
                                                            <svg xmlns="http://www.w3.org/2000/svg" width="25" height="25" fill="red" class="bi bi-pin-angle" viewBox="0 0 16 16">
                                                                <path d="M9.828.722a.5.5 0 0 1 .354.146l4.95 4.95a.5.5 0 0 1 0 .707c-.48.48-1.072.588-1.503.588-.177 0-.335-.018-.46-.039l-3.134 3.134a6 6 0 0 1 .16 1.013c.046.702-.032 1.687-.72 2.375a.5.5 0 0 1-.707 0l-2.829-2.828-3.182 3.182c-.195.195-1.219.902-1.414.707s.512-1.22.707-1.414l3.182-3.182-2.828-2.829a.5.5 0 0 1 0-.707c.688-.688 1.673-.767 2.375-.72a6 6 0 0 1 1.013.16l3.134-3.133a3 3 0 0 1-.04-.461c0-.43.108-1.022.589-1.503a.5.5 0 0 1 .353-.146m.122 2.112v-.002zm0-.002v.002a.5.5 0 0 1-.122.51L6.293 6.878a.5.5 0 0 1-.511.12H5.78l-.014-.004a5 5 0 0 0-.288-.076 5 5 0 0 0-.765-.116c-.422-.028-.836.008-1.175.15l5.51 5.509c.141-.34.177-.753.149-1.175a5 5 0 0 0-.192-1.054l-.004-.013v-.001a.5.5 0 0 1 .12-.512l3.536-3.535a.5.5 0 0 1 .532-.115l.096.022c.087.017.208.034.344.034q.172.002.343-.04L9.927 2.028q-.042.172-.04.343a1.8 1.8 0 0 0 .062.46z" />
                                                            </svg>
                                                        }
                                                    </button>
                                                </form>
                                            }

                                            <!-- Nút Like -->
                                            <form method="post" asp-page-handler="Like">
                                                @Html.AntiForgeryToken()
                                                <input name="id" hidden value="@i.ForumId" />
                                                <button class="like-button" data-id="@i.ForumId"
                                                        style="border:none; background-color:#ffbf00; border-radius:50%; width: 2rem; height: 2rem; padding-top: 8%; padding-left: 20%;">
                                                    <i class="bi bi-suit-heart-fill fs-5"></i>
                                                </button>
                                            </form>
                                        </div>
                                    </div>
                                    <p>
                                        @Html.Raw(i.Content.Replace("\\r\\n", "<br>"))
                                    </p>
                                </div>
                            </a>
                            <div style="padding-bottom: 20px;">
                                <div class="text-start forum_react"
                                     style="padding-left: 22px; display: flex; justify-content: space-between;">
                                    <div style="display: flex;">
                                        <img src="@Model.InformationUser.Image" width="30px" height="30px"
                                             style="border-radius: 50%; margin-right: 10px; margin-top: 5px;" />
                                        <div>
                                            <h6 style="margin: 0;">@Model.InformationUser.Fullname</h6>
                                            @{
                                                TimeSpan timeDiff = i.CreateAt.HasValue ? (DateTime.Now - i.CreateAt.Value) : TimeSpan.Zero;
                                                string timeDisplay;

                                                if (timeDiff.TotalMinutes < 60)
                                                {
                                                    timeDisplay = $"{(int)timeDiff.TotalMinutes} phút trước";
                                                }
                                                else if (timeDiff.TotalHours < 24)
                                                {
                                                    timeDisplay = $"{(int)timeDiff.TotalHours} giờ trước";
                                                }
                                                else
                                                {
                                                    timeDisplay = $"{(int)timeDiff.TotalDays} ngày trước";
                                                }
                                            }
                                            <p style="margin: 0;">@timeDisplay</p>
                                        </div>
                                    </div>
                                    <div class="react" style="display: flex; justify-content: space-between; margin-right: 22px; width: 60%;">
                                        <div class="m-auto">@i.Views Views</div>
                                        <div class="like-count m-auto">@i.Likes Likes</div>
                                        <div class="m-auto">@i.Comments comments</div>
                                    </div>
                                </div>
                            </div>
                        </div>
                    }
                </div>

            </div>
        }
    </div>

    <style>
        /* Scoped to Profile Page */
        .profile-page .container {
            max-width: 900px;
            margin: 0 auto;
            padding: 20px;
            background-color: #fff;
            border-radius: 10px;
            box-shadow: 0 0 15px rgba(0, 0, 0, 0.1);
            margin-top: 50px;
        }

        .profile-page h1 {
            text-align: center;
            color: #333;
            margin-bottom: 20px;
        }

        .profile-page .profile-header {
            display: flex;
            align-items: center;
            justify-content: space-between; /* Center-align image and profile info */
            flex-wrap: wrap;
            gap: 20px;
            padding: 20px;
        }

            /* Profile Picture */
            .profile-page .profile-header img {
                width: 150px;
                height: 150px;
                border-radius: 50%;
                object-fit: cover;
                margin-left: 100px;
            }

        /* Profile Info */
        .profile-page .profile-info {
            flex: 1;
            max-width: 500px;
            padding: 20px;
            text-align: left;
            /*  margin-left: 150px; */ /* Shift to the right */
        }

            .profile-page .profile-info p {
                font-size: 16px;
                margin: 8px 0;
            }

                .profile-page .profile-info p span {
                    font-weight: bold;
                    color: #555;
                }
    </style>

    <script>
        document.getElementById("updateProfileCloseBtn").addEventListener("click", function () {
            var form = document.getElementById("updateProfileForm");
            var button = document.getElementById("updateProfileCloseBtn");

            if (form.style.display === "none" || form.style.display === "") {
                form.style.display = "block";
                button.innerText = "Đóng cập nhật";
            } else {
                form.style.display = "none";
                button.innerText = "Cập nhật";
            }
        });
    </script>
    <script>
        document.addEventListener("DOMContentLoaded", function () {
            var toastElement = document.getElementById("toastNotification");
            var progressBar = document.createElement('div');
            progressBar.classList.add("toast-progress-bar");
            progressBar.style = "position: absolute; bottom: 0; left: 0; height: 5px; background-color: orange;";
            toastElement.appendChild(progressBar);

            var duration = 4000;
            var startTime = Date.now();

            if (toastElement) {
                var interval = setInterval(function () {
                    var elapsedTime = Date.now() - startTime;
                    var progress = (elapsedTime / duration) * 100;

                    progressBar.style.width = progress + "%";

                    if (elapsedTime >= duration) {
                        clearInterval(interval);
                        var toast = new bootstrap.Toast(toastElement);
                        toast.hide();
                    }
                }, 100);
            }
        });
    </script>

    <script>
        function toggleSubmitButton() {
            var fullnameInput = document.getElementById("Fullname");
            var phoneInput = document.getElementById("Phone");
            var addressInput = document.getElementById("Address");

            var submitButton = document.getElementById("updateProfileBtn");

            var isValidFullname = fullnameInput.value.length > 6 && /^[a-zA-Zàáảãạăằắẳẵặâầấẩẫậêềếểễệôồốổỗộơờớỡởợưừứửữựíìỉĩịóòỏõọôồốổỗộuùúũủụyỳýỷỹỵđ\s-]+$/.test(fullnameInput.value);
            var isValidPhone = (phoneInput.value.length == 10 && /^(09|08|03|07|05)\d{8}$/.test(phoneInput.value)) || phoneInput.value.length == 0;
            var isValidAddress = addressInput.value.length > 10 && /^[a-zA-Z0-9àáảãạăằắẳẵặâầấẩẫậêềếểễệôồốổỗộơờớỡởợưừứửữựíìỉĩịóòỏõọôồốổỗộuùúũủụyỳýỷỹỵđ\s\-\(\)_\+=\[\]\{\};:,\./|]+$/.test(addressInput.value);

            if (isValidFullname && isValidPhone) {
                submitButton.style.display = "inline-block";
            } else {
                submitButton.style.display = "none";
            }
        }

        document.getElementById("Fullname").addEventListener("input", function () {
            var input = this.value;
            var error = document.getElementById("fullnameError");

            var validNamePattern = /^[a-zA-Zàáảãạăằắẳẵặâầấẩẫậêềếểễệôồốổỗộơờớỡởợưừứửữựíìỉĩịóòỏõọôồốổỗộuùúũủụyỳýỷỹỵđ\s-]+$/;

            if (input.length <= 6 || !validNamePattern.test(input)) {
                error.style.display = "block";
                this.style.borderColor = "red";
            } else {
                error.style.display = "none";
                this.style.borderColor = "";
            }
            toggleSubmitButton();
        });

        document.getElementById("Phone").addEventListener("input", function () {
            var input = this.value;
            var error = document.getElementById("phoneError");

            var validPhonePattern = /^(09|08|03|07|05)\d{8}$/;

            if (input.length != 0) {
                if (!validPhonePattern.test(input) || input.length != 10) {
                    error.style.display = "block";
                    this.style.borderColor = "red";
                } else {
                    error.style.display = "none";
                    this.style.borderColor = "";
                }
            } else {
                error.style.display = "none";
                this.style.borderColor = "";
            }
            toggleSubmitButton();
        });

    </script>
    <!-- Template Javascript -->
    <script src="~/js/main.js"></script>
</body>

</html>

<script>
    document.addEventListener("DOMContentLoaded", function () {
        document.querySelectorAll(".like-button").forEach(button => {
            button.addEventListener("click", async function (event) {
                event.preventDefault();

                let postId = this.getAttribute("data-id");
                let likeCountElement = this.closest(".forum-item").querySelector(".like-count");

                let csrfToken = document.querySelector("input[name='__RequestVerificationToken']")?.value || "";

                let response = await fetch(`/Forum?handler=Like`, {
                    method: "POST",
                    headers: {
                        "Content-Type": "application/json",
                        "RequestVerificationToken": csrfToken
                    },
                    body: JSON.stringify({ id: postId })
                });

                if (response.ok) {
                    let result = await response.json();
                    if (result.success) {
                        likeCountElement.innerText = `${result.likes} Likes`;
                    } else {
                        alert(result.message);
                    }
                } else {
                    let errorText = await response.text();
                    alert(`Lỗi: ${response.status} - ${errorText}`);
                }
            });
        });
    });

</script>
